import { Poppins  , Roboto   } from 'next/font/google';

export const head = Poppins({weight: ["400", "700"], subsets: ['latin'] });
export const body = Roboto({weight: ["400", "700"], subsets: ['latin'] });
